# Vision Agents SDK + Plugins (the AI agent that joins Stream Video calls)
vision-agents[getstream,gemini,deepgram,elevenlabs,ultralytics]

# Web server — serves the frontend-facing WebSocket + REST
fastapi[standard]
uvicorn[standard]
websockets

# Environment
python-dotenv

# Computer Vision (used inside SpeakingCoachProcessor for per-frame analysis)
opencv-python-headless
numpy
mediapipe

# PyAV — required by Vision Agents SDK for av.VideoFrame
av

# WebRTC — required by Vision Agents SDK edge transport
aiortc
aiortc
